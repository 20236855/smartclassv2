# 项目相关配置
ruoyi:
  # 名称
  name: 智慧课程系统
  # 版本
  version: 1.0.0
  # 版权年份
  copyrightYear: 2025
  # 文件路径 示例（ Windows配置D:/ruoyi/uploadPath，Linux配置 /home/ruoyi/uploadPath）
  profile: C:/ruoyi/uploadPath
  # 获取ip地址开关
  addressEnabled: false
  # 验证码类型 math 数字计算 char 字符验证
  captchaType: math

# 开发环境配置
server:
  # 服务器的HTTP端口，默认为8080
  port: 8080
  servlet:
    # 应用的访问路径
    context-path: /
  tomcat:
    # tomcat的URI编码
    uri-encoding: UTF-8
    # 连接数满后的排队数，默认为100
    accept-count: 1000
    threads:
      # tomcat最大线程数，默认为200
      max: 800
      # Tomcat启动初始化的线程数，默认值10
      min-spare: 100

# 日志配置
logging:
  level:
    com.ruoyi: debug
    org.springframework: warn

# 用户配置
user:
  password:
    # 密码最大错误次数
    maxRetryCount: 5
    # 密码锁定时间（默认10分钟）
    lockTime: 10

# Spring配置
spring:
  # 资源信息
  messages:
    # 国际化资源文件路径
    basename: i18n/messages
  profiles:
    active: druid
  # 邮箱配置（QQ邮箱）
  mail:
    host: smtp.qq.com
    port: 587
    username: 1320774739@qq.com
    password: zcvizozhbxchggid
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
  # 文件上传
  servlet:
    multipart:
      # 单个文件大小（支持大视频文件上传）
      max-file-size: 1024MB
      # 设置总上传的文件大小
      max-request-size: 1024MB
  # 服务模块
  devtools:
    restart:
      # 热部署开关
      enabled: true
  # redis 配置
  redis:
    # 地址
    host: localhost
    # 端口，默认为6379
    port: 6379
    # 数据库索引
    database: 0
    # 密码
    password:
    # 连接超时时间
    timeout: 10s
    lettuce:
      pool:
        # 连接池中的最小空闲连接
        min-idle: 0
        # 连接池中的最大空闲连接
        max-idle: 8
        # 连接池的最大数据库连接数
        max-active: 8
        # #连接池最大阻塞等待时间（使用负值表示没有限制）
        max-wait: -1ms

# token配置
token:
  # 令牌自定义标识
  header: Authorization
  # 令牌密钥
  secret: abcdefghijklmnopqrstuvwxyz
  # 令牌有效期（默认30分钟）
  expireTime: 30

# MyBatis配置
mybatis:
  # 搜索指定包别名
  typeAliasesPackage: com.ruoyi.**.domain
  # 配置mapper的扫描，找到所有的mapper.xml映射文件
  mapperLocations: classpath*:mapper/**/*Mapper.xml
  # 加载全局的配置文件
  configLocation: classpath:mybatis/mybatis-config.xml

# PageHelper分页插件
pagehelper:
  helperDialect: mysql
  supportMethodsArguments: true
  params: count=countSql

# Swagger配置
swagger:
  # 是否开启swagger
  enabled: true
  # 请求前缀
  pathMapping: /dev-api

# 防止XSS攻击
xss:
  # 过滤开关
  enabled: true
  # 排除链接（多个用逗号分隔）
  excludes: /system/notice
  # 匹配链接
  urlPatterns: /system/*,/monitor/*,/tool/*

# 微调模型API配置
finetuned:
  # 个性化推荐模型
  recommendation:
    url: "https://u811610-8a58-f821b323.westb.seetacloud.com:8443/recommendation_generate"
  # 知识图谱生成模型
  kg:
    url: "https://u811610-8a58-f821b323.westb.seetacloud.com:8443/kg_generate"


# 通义千问API配置（原配置保留）
tongyi:
  qianwen:
    api-key: sk-92d17cdcbcd74bb4938ce53ac333d25c
    model: qwen-turbo  # 轻量版足够用
    timeout: 30000  # 超时时间30秒（避免报告生成慢导致超时）

# AI 服务配置（新增配置）
qianwen:
  api:
    # 已硬编码（注意安全）：将下面两个字段替换为你的真实 endpoint 与 key
    url: "https://api.siliconflow.cn/v1/chat/completions"
    key: "sk-kliintzjxqkxwjxdjvbuacpnshemgdnjtmqjcbxumkxrjvnz"
    # 指定模型（默认 Qwen/QwQ-32B）
    model: "Qwen/Qwen3-8B"

# 视频转录配置（新增配置）
video:
  transcription:
    # 转录服务提供商：whisper（免费推荐）, aliyun（阿里云）, tencent（腾讯云）, baidu（百度）, none（不转录）
    provider: "whisper"
    # 是否启用转录
    enabled: true

# Whisper服务配置（完全免费）（新增配置）
whisper:
  service:
    # Whisper服务地址
    url: "http://localhost:8002"
  # 模型大小：tiny（最快，85%准确率）, base（推荐，95%准确率）, small, medium, large（最准确）
  # 使用tiny模型：速度最快，准确率稍低但可接受
  model: "tiny"
  # 超时时间（毫秒），默认30分钟（处理长视频）
  timeout: 1800000


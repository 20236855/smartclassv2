# 更新课程图片指南

## 📋 概述

本指南说明如何为8门课程添加/更新封面图片。

## 🎯 课程列表

| ID | 课程名称 | 建议图片名称 |
|----|---------|-------------|
| 1 | 前端开发基础 | course-1.jpg |
| 2 | JavaScript高级编程 | course-2.jpg |
| 3 | Vue.js框架开发 | course-3.jpg |
| 4 | React框架实战 | course-4.jpg |
| 5 | 前端工程化实践 | course-5.jpg |
| 6 | Node.js后端开发 | course-6.jpg |
| 7 | 移动端开发 | course-7.jpg |
| 8 | 前端性能优化 | course-8.jpg |

## 📁 文件存储位置

**服务器路径**：`D:\ruoyi\uploadPath\courses\`

**访问URL**：`http://localhost:8080/profile/courses/图片名称.jpg`

## 🚀 操作步骤

### 步骤1：创建目录

在PowerShell中运行：

```powershell
cd RuoYi-Vue-master\scripts
.\setup_course_images.ps1
```

或者手动创建目录：
```powershell
New-Item -ItemType Directory -Path "D:\ruoyi\uploadPath\courses" -Force
```

### 步骤2：准备图片

**图片要求**：
- 格式：JPG、PNG、WEBP等
- 尺寸：建议 800x600 或 16:9 比例
- 大小：每张图片不超过 2MB
- 命名：course-1.jpg 到 course-8.jpg

**复制图片到目录**：
```powershell
# 示例：从您的图片文件夹复制
Copy-Item "C:\Users\Lenovo\Pictures\我的课程图片\*.jpg" -Destination "D:\ruoyi\uploadPath\courses\"
```

### 步骤3：更新数据库

执行SQL脚本：

```bash
mysql -uroot -p123456 smartclassv2 < RuoYi-Vue-master\sql\update_course_images.sql
```

或者在MySQL命令行中：
```sql
source RuoYi-Vue-master/sql/update_course_images.sql;
```

### 步骤4：验证

**检查数据库**：
```sql
SELECT id, title, cover_image FROM course ORDER BY id;
```

**检查文件**：
```powershell
Get-ChildItem "D:\ruoyi\uploadPath\courses\"
```

**浏览器测试**：
1. 刷新浏览器（Ctrl + Shift + R）
2. 点击"题目练习"菜单
3. 查看课程卡片是否显示图片

## 🔧 方案选择

### 方案A：本地文件（推荐）

优点：
- 加载速度快
- 不依赖外网
- 可以自定义图片

步骤：按照上述步骤操作

### 方案B：使用网络图片

如果您想使用在线图片，修改SQL脚本：

```sql
UPDATE course SET cover_image = 'https://example.com/images/course1.jpg' WHERE id = 1;
UPDATE course SET cover_image = 'https://example.com/images/course2.jpg' WHERE id = 2;
-- 等等...
```

### 方案C：使用占位图片服务

临时使用占位图片：

```sql
UPDATE course SET cover_image = 'https://via.placeholder.com/800x600/4A90E2/FFFFFF?text=前端开发基础' WHERE id = 1;
UPDATE course SET cover_image = 'https://via.placeholder.com/800x600/E94B3C/FFFFFF?text=JavaScript高级' WHERE id = 2;
-- 等等...
```

## 🎨 前端显示

课程图片在前端的显示代码（已实现）：

```vue
<el-image 
  :src="course.coverImage" 
  fit="cover"
  class="course-cover"
>
  <div slot="error" class="image-slot">
    <i class="el-icon-picture-outline"></i>
  </div>
</el-image>
```

## ❓ 常见问题

### Q1: 图片不显示怎么办？

**检查清单**：
1. 文件是否存在：`D:\ruoyi\uploadPath\courses\course-1.jpg`
2. 数据库路径是否正确：`/profile/courses/course-1.jpg`
3. 后端服务是否运行
4. 浏览器是否缓存（强制刷新：Ctrl + Shift + R）

### Q2: 如何批量重命名图片？

PowerShell脚本：
```powershell
cd "D:\ruoyi\uploadPath\courses"
$i = 1
Get-ChildItem *.jpg | ForEach-Object {
    Rename-Item $_ -NewName "course-$i.jpg"
    $i++
}
```

### Q3: 如何使用不同的图片格式？

修改SQL脚本中的扩展名：
```sql
UPDATE course SET cover_image = '/profile/courses/course-1.png' WHERE id = 1;
```

### Q4: 图片太大怎么办？

使用图片压缩工具：
- TinyPNG: https://tinypng.com/
- Squoosh: https://squoosh.app/

## 📝 注意事项

1. **路径格式**：数据库中使用 `/profile/courses/xxx.jpg`，不是 `D:\ruoyi\uploadPath\courses\xxx.jpg`
2. **文件权限**：确保目录有读写权限
3. **图片大小**：建议每张图片不超过 2MB
4. **缓存问题**：更新图片后需要强制刷新浏览器

## 🎉 完成

完成以上步骤后，课程卡片应该显示您的自定义图片了！

